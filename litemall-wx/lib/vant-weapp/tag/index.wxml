<wxs src="../wxs/org.linlinjava.canteen.redis.utils.wxs" module="org.linlinjava.canteen.redis.utils" />
<wxs src="./index.wxs" module="computed" />

<view
  class="custom-class {{ org.linlinjava.canteen.redis.utils.bem('tag', [type, size, { mark, plain, round }]) }}"
  style="{{ computed.rootStyle({ plain, color, textColor }) }}"
>
  <slot />
  <van-icon
    wx:if="{{ closeable }}"
    name="cross"
    custom-class="van-tag__close"
    bind:click="onClose"
  />
</view>
