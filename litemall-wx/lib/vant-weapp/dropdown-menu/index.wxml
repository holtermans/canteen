<wxs src="../wxs/org.linlinjava.canteen.redis.utils.wxs" module="org.linlinjava.canteen.redis.utils" />
<wxs src="./index.wxs" module="computed" />

<view class="van-dropdown-menu van-dropdown-menu--top-bottom">
  <view
    wx:for="{{ itemListData }}"
    wx:key="index"
    data-index="{{ index }}"
    class="{{ org.linlinjava.canteen.redis.utils.bem('dropdown-menu__item', { disabled: item.disabled }) }}"
    bind:tap="onTitleTap"
  >
    <view
      class="{{ item.titleClass }} {{ org.linlinjava.canteen.redis.utils.bem('dropdown-menu__title', { active: item.showPopup, down: item.showPopup === (direction === 'down') }) }}"
      style="{{ item.showPopup ? 'color:' + activeColor : '' }}"
    >
      <view class="van-ellipsis">
        {{ computed.displayTitle(item) }}
      </view>
    </view>
  </view>

  <slot />
</view>
